# Production / MVP target: PostgreSQL
DATABASE_URL=postgresql+psycopg2://cpt:cpt@127.0.0.1:5432/cpt

# Zero-deps local fallback (comment out Postgres URL above):
# DATABASE_URL=sqlite:///./data/cpt.db
