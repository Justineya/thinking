services:
  cpt:
    image: python:3.12-slim
    working_dir: /app
    volumes:
      - ./:/app
    command: bash -lc "pip install -r requirements.txt -q && PYTHONPATH=. uvicorn backend.app.main:app --host 0.0.0.0 --port 8787"
    ports:
      - "8787:8787"
